<?xml version="1.0" encoding="utf-8"?>
<resources>

    <attr name="slidingButtonStyle" format="reference" />

    <declare-styleable name="SlidingButton">
        <attr name="thumb" format="reference" />
        <attr name="track" format="reference" />
        <attr name="backgroundOff" format="reference" />
        <attr name="backgroundOffDisable" format="reference" />
        <attr name="backgroundOn" format="reference" />
        <attr name="backgroundOnDisable" format="reference" />
        <attr name="buttonOff" format="reference" />
        <attr name="buttonOffDisable" format="reference" />
        <attr name="buttonOn" format="reference" />
        <attr name="buttonOnDisable" format="reference" />
    </declare-styleable>

    <attr name="slidingButtonPreferenceStyle" format="reference" />

    <attr name="toastTextAppearance" format="reference" />

    <!-- Base attributes available to SlidingButtonPreference. -->
    <declare-styleable name="SlidingButtonPreference">

        <!--
             The summary for the Preference in a PreferenceActivity screen when the
             SlidingButtonPreference is checked. If separate on/off summaries are not
             needed, the summary attribute can be used instead.
        -->
        <attr name="summaryOn" format="string" />
        <!--
             The summary for the Preference in a PreferenceActivity screen when the
             SlidingButtonPreference is unchecked. If separate on/off summaries are not
             needed, the summary attribute can be used instead.
        -->
        <attr name="summaryOff" format="string" />
        <!--
             The state (true for on, or false for off) that causes dependents to be disabled. By default,
             dependents will be disabled when this is unchecked, so the value of this preference is false.
        -->
        <attr name="disableDependentsState" format="boolean" />
    </declare-styleable>

    <!-- IKPHONE-726, FengYibin, 2013.11.02, for marginLeft of listMenuItem style. -->
    <declare-styleable name="ListMenuItem">
        <attr name="listMenuItemLayout" format="reference" />
    </declare-styleable>

    <!-- Default style for the VerticalSeekBar widget. -->
    <attr name="VerticalSeekBarStyle" format="reference" />

    <declare-styleable name="VerticalSeekBar">
        <attr name="progressDrawable" format="reference" />
        <attr name="thumb" />
        <attr name="icon" format="reference" />
        <attr name="indeterminateDrawable" format="reference" />
    </declare-styleable>

    <!-- Default style for the Sn1HorSeekBar widget. -->
    <attr name="Sn1HorSeekBarStyle" format="reference" />

    <declare-styleable name="Sn1HorSeekBar">
        <attr name="progressDrawable" />
        <attr name="thumb" />
        <attr name="icon" />
        <attr name="indeterminateDrawable" />
    </declare-styleable>
    <declare-styleable name="Theme">

        <!-- The DatePicker style. -->
        <attr name="datePickerStyle" format="reference" />
        <!-- NumberPicker style. -->
        <attr name="numberPickerStyle" format="reference" />
        <!-- The TimePicker style. -->
        <attr name="timePickerStyle" format="reference" />
    </declare-styleable>
    <declare-styleable name="DatePicker">

        <!--
             The first year (inclusive), for example "1940".
             {@deprecated Use minDate instead.}
        -->
        <attr name="startYear" format="integer" />
        <!--
             The last year (inclusive), for example "2010".
             {@deprecated Use maxDate instead.}
        -->
        <attr name="endYear" format="integer" />
        <!-- Whether the spinners are shown. -->
        <attr name="spinnersShown" format="boolean" />
        <!-- Whether the calendar view is shown. -->
        <attr name="calendarViewShown" format="boolean" />
        <!-- The minimal date shown by this calendar view in mm/dd/yyyy format. -->
        <attr name="minDate" format="string" />
        <!-- The maximal date shown by this calendar view in mm/dd/yyyy format. -->
        <attr name="maxDate" format="string" />
        <!-- @hide The layout of the date picker. -->
        <attr name="internalLayout" format="reference" />
    </declare-styleable>
    <declare-styleable name="NumberPicker">

        <!-- @hide Color for the solid color background if such for optimized rendering. -->
        <attr name="solidColor" format="color|reference" />
        <!-- @hide The divider for making the selection area. -->
        <attr name="selectionDivider" format="reference" />
        <!-- @hide The height of the selection divider. -->
        <attr name="selectionDividerHeight" format="dimension" />
        <!-- @hide The distance between the two selection dividers. -->
        <attr name="selectionDividersDistance" format="dimension" />
        <!-- @hide The min height of the NumberPicker. -->
        <attr name="internalMinHeight" format="dimension" />
        <!-- @hide The max height of the NumberPicker. -->
        <attr name="internalMaxHeight" format="dimension" />
        <!-- @hide The min width of the NumberPicker. -->
        <attr name="internalMinWidth" format="dimension" />
        <!-- @hide The max width of the NumberPicker. -->
        <attr name="internalMaxWidth" format="dimension" />
        <!-- @hide The layout of the number picker. -->
        <attr name="internalLayout" />
        <!-- @hide The drawable for pressed virtual (increment/decrement) buttons. -->
        <attr name="virtualButtonPressedDrawable" format="reference" />
        <!-- @hide The flag of using SuningUI // SuningUI 13070107 -->
        <attr name="SuningWidget" format="boolean" />
        <!-- @hide The adjust distance of the text lines. // SuningUI 13070107 -->
        <attr name="adjustDistance" format="dimension" />
        <!-- @hide The text of the tip notice // SuningUI 13070107 -->
        <attr name="tipNoticeText" format="string" />
    </declare-styleable>
    <declare-styleable name="TimePicker">

        <!-- @hide The layout of the time picker. -->
        <attr name="internalLayout" />
    </declare-styleable>

    <declare-styleable name="AnalogClock">
        <attr name="dial" format="reference"/>
        <attr name="hand_hour" format="reference"/>
        <attr name="hand_minute" format="reference"/>
        <attr name="hand_second" format="reference" />
    </declare-styleable>

</resources>